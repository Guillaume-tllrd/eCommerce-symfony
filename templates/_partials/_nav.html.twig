<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('main')}}">e-commerce</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{path('main')}}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('products_index')}}">Produits</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('categories_index')}}">Catégories</a>
				</li>
			</ul>
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				{% if app.user %}
					{# pour que administration soit visible unqiuement par les admin il faut utiliser la fonction is_granted #}
					{% if is_granted('ROLE_PRODUCT_ADMIN') %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('admin_index') }}">Administration</a>
						</li>
					{% endif %}

					<li class="nav-item">
						<a href="{{path("cart_index")}}" class="nav-link">Panier<i class="bi bi-amazon"></i>
							{# <span>({{ totalQuantity }})</span> #}
						</a>
					</li>
					{# on met la function twig path, on appelle la function logout dans le securityController#}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_logout')}}">Me déconnecter</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_login')}}">Me connecter</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_register')}}">M'inscrire</a>
					</li>
				{% endif %}
			</ul>

		</div>
	</div>
</nav>

<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container">
		<form class="d-flex mx-auto" role="search">
			<input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
			<button class="btn btn-outline-success" type="submit">Search</button>
		</form>
	</div>
</nav>
